# 🔧 Настройка Git и загрузка кода

## Проблема

Вы получили ошибку при попытке сделать commit, потому что Git не знает ваше имя и email.

## Решение

Выполните следующие команды в **Git Bash** (не в PowerShell):

### Вариант 1: Быстрая настройка

```bash
# Настройте имя и email
git config --global user.name "Coder2429"
git config --global user.email "coder2429@users.noreply.github.com"

# Теперь создайте коммит
git add .
git commit -m "Initial commit"

# И отправьте в GitHub
git push -u origin main
```

### Вариант 2: Использование скрипта

Или выполните скрипт `setup-git.sh` в Git Bash:

```bash
bash setup-git.sh
```

## После успешного push

1. Перейдите на GitHub: https://github.com/Coder2429/qwerty
2. Убедитесь, что код загружен
3. Настройте GitHub Pages (см. `DEPLOY_GUIDE.md`)

## Важно

- Используйте **Git Bash**, а не PowerShell или обычный терминал Windows
- Email можно использовать любой (даже несуществующий), но лучше использовать GitHub noreply email
- После настройки `--global` эти настройки будут использоваться для всех ваших репозиториев

