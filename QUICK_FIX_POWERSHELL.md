# 🔧 Быстрое решение для PowerShell

## Проблема: npm не найден в PowerShell

Если в CMD `node` и `npm` работают, но в PowerShell нет - это проблема с PATH в PowerShell.

## ✅ Решение 1: Использовать CMD (рекомендуется)

Просто используйте **Командную строку (CMD)** вместо PowerShell:

1. Нажмите `Win + R`
2. Введите `cmd` и нажмите Enter
3. Все команды будут работать нормально

## ✅ Решение 2: Добавить Node.js в PATH для PowerShell

Если хотите использовать PowerShell, добавьте путь в PATH:

### Временное решение (для текущей сессии):

В PowerShell выполните:
```powershell
$env:Path += ";C:\Program Files\nodejs\"
```

Теперь `npm` будет работать в этой сессии PowerShell.

### Постоянное решение:

1. Нажмите `Win + R`
2. Введите `sysdm.cpl` и нажмите Enter
3. Перейдите на вкладку **"Дополнительно"**
4. Нажмите **"Переменные среды"**
5. В разделе **"Системные переменные"** найдите **"Path"**
6. Нажмите **"Изменить"**
7. Проверьте, есть ли там: `C:\Program Files\nodejs\`
8. Если нет - нажмите **"Создать"** и добавьте: `C:\Program Files\nodejs\`
9. Нажмите **"OK"** везде
10. **Перезапустите PowerShell**

## ✅ Решение 3: Перезагрузить компьютер

После установки Node.js иногда нужно перезагрузить компьютер, чтобы PATH обновился во всех приложениях.

---

## 📝 После решения проблемы

Когда `npm` работает, установите зависимости:

```cmd
cd "D:\Рабочии стол\Vkbot\backend"
npm install
```

Затем запустите сервер:

```cmd
npm start
```

