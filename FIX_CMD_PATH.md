# 🔧 Исправление PATH в CMD

## Проблема: "node не является внутренней или внешней командой" в CMD

Если Node.js установлен, но команды не работают в CMD - это проблема с PATH.

## ✅ Решение 1: Перезапустить CMD (самое простое)

1. **Закройте все окна CMD**
2. Откройте **новое** окно CMD
3. Выполните:
   ```cmd
   node --version
   ```

Если это не помогло, используйте Решение 2.

## ✅ Решение 2: Добавить путь в текущую сессию CMD

В открытом окне CMD выполните:

```cmd
set PATH=%PATH%;C:\Program Files\nodejs\
```

Затем проверьте:
```cmd
node --version
npm --version
```

**Примечание:** Это работает только для текущего окна CMD. После закрытия окна нужно будет сделать снова.

## ✅ Решение 3: Добавить путь в системный PATH (постоянно)

1. Нажмите `Win + R`
2. Введите `sysdm.cpl` и нажмите Enter
3. Перейдите на вкладку **"Дополнительно"**
4. Нажмите **"Переменные среды"**
5. В разделе **"Системные переменные"** найдите переменную **"Path"**
6. Нажмите **"Изменить"**
7. Проверьте, есть ли там: `C:\Program Files\nodejs\`
8. Если нет:
   - Нажмите **"Создать"**
   - Введите: `C:\Program Files\nodejs\`
   - Нажмите **"OK"**
9. Нажмите **"OK"** везде
10. **Закройте все окна CMD и откройте новое**

## ✅ Решение 4: Перезагрузить компьютер

После установки Node.js иногда нужно перезагрузить компьютер, чтобы PATH обновился во всех приложениях.

## 🔍 Проверка установки Node.js

Проверьте, установлен ли Node.js:

1. Откройте Проводник
2. Перейдите в `C:\Program Files\nodejs\`
3. Должны быть файлы: `node.exe` и `npm.cmd`

Если файлов нет - Node.js не установлен. См. [INSTALL_NODEJS.md](./INSTALL_NODEJS.md)

## 📝 После исправления PATH

Когда `node` и `npm` работают, выполните:

```cmd
cd "D:\Рабочии стол\Vkbot\backend"
npm start
```

