# 🔧 Решение проблем

## Предупреждение VK при копировании токена

### Проблема
При попытке скопировать токен из адресной строки VK показывает предупреждение:
> "Пожалуйста, не копируйте данные из адресной строки для сторонних сайтов. Таким образом Вы можете потерять доступ к Вашему аккаунту."

### Это безопасно?
**Да, в данном случае это безопасно**, потому что:
- Вы находитесь на официальном домене VK (`oauth.vk.com`)
- Вы получаете токен для **своего собственного приложения**
- Это стандартный официальный способ получения токена для разработки

### Решение
**Вариант 1: Игнорировать предупреждение**
- Это предупреждение VK для защиты от фишинга
- В нашем случае (официальный домен VK, ваше приложение) это безопасно
- Можете продолжить копирование токена из адресной строки

**Вариант 2: Использовать консоль браузера**
1. После редиректа на `oauth.vk.com/blank.html` нажмите **F12**
2. Перейдите на вкладку **"Console"**
3. Введите команду:
   ```javascript
   window.location.hash.match(/access_token=([^&]+)/)?.[1]
   ```
4. Нажмите Enter — токен будет выведен в консоли
5. Скопируйте токен из консоли

**Вариант 3: Использовать расширение браузера**
- Установите расширение для работы с URL параметрами
- Или используйте онлайн-инструменты для парсинга URL

---

## Ошибка "invalid scope" при получении токена

### Проблема
При попытке получить токен VK API вы видите ошибку:
```json
{"error":"invalid_request","error_description":"invalid scope"}
```

### Причина
VK API требует **числовые коды** для scope (прав доступа), а не текстовые названия.

### Решение

**Неправильно (вызовет ошибку):**
```
scope=photos,wall,groups,offline
```

**Правильно (используйте числовые коды):**
```
scope=4,8,268435456
```

### Правильная ссылка для получения токена

Замените `YOUR_APP_ID` на ID вашего приложения:

```
https://oauth.vk.com/authorize?client_id=YOUR_APP_ID&display=page&redirect_uri=https://oauth.vk.com/blank.html&scope=4,8,268435456&response_type=token&v=5.131
```

### Объяснение scope (прав доступа)

- `4` = **photos** - доступ к фотографиям (для загрузки фото в посты)
- `8` = **wall** - доступ к стене (для публикации постов)
- `268435456` = **offline** - доступ в любое время (токен без срока действия)

### Пример с реальным ID приложения

Если ID вашего приложения = `54439677`, используйте:

```
https://oauth.vk.com/authorize?client_id=54439677&display=page&redirect_uri=https://oauth.vk.com/blank.html&scope=4,8,268435456&response_type=token&v=5.131
```

---

## Другие частые проблемы

### Backend не запускается

**Проблема:** Ошибка при запуске сервера

**Решения:**
1. Проверьте, что порт 5000 свободен:
   ```bash
   # Windows
   netstat -ano | findstr :5000
   
   # Linux/Mac
   lsof -i :5000
   ```

2. Убедитесь, что установлены все зависимости:
   ```bash
   cd backend
   npm install
   ```

3. Проверьте файл `.env` - все переменные должны быть заполнены

### Ошибка "VK_ACCESS_TOKEN не установлен"

**Проблема:** Backend не может найти токен VK

**Решение:**
1. Убедитесь, что файл `backend/.env` существует
2. Проверьте, что в файле есть строка:
   ```
   VK_ACCESS_TOKEN=ваш_токен_здесь
   ```
3. Токен должен быть без кавычек и пробелов
4. Перезапустите backend после изменения `.env`

### Ошибка при публикации поста

**Проблема:** Пост не публикуется, ошибка в логах

**Решения:**
1. Проверьте права токена - должны быть: photos, wall, groups, offline
2. Убедитесь, что токен имеет права администратора группы
3. Проверьте логи: `backend/logs/error.log`
4. Убедитесь, что группа существует и доступна

### VK Pay не работает

**Проблема:** Оплата не проходит или не открывается

**Решения:**
1. Убедитесь, что VK Pay включён в настройках приложения (dev.vk.com)
2. Проверьте, что приложение опубликовано (не в режиме разработки)
3. Убедитесь, что используете правильный формат заказа
4. Проверьте консоль браузера (F12) на наличие ошибок

### Frontend не подключается к Backend

**Проблема:** Ошибка CORS или "Failed to fetch"

**Решения:**
1. Проверьте, что backend запущен на порту 5000
2. Убедитесь, что в `backend/.env` указан правильный `FRONTEND_URL`
3. Проверьте `frontend/.env` - должен быть указан `VITE_API_URL=http://localhost:5000`
4. Перезапустите оба сервера после изменения `.env` файлов

### База данных не создаётся

**Проблема:** Ошибка при создании БД

**Решения:**
1. Убедитесь, что у приложения есть права на запись в папку `backend/data/`
2. Запустите инициализацию вручную:
   ```bash
   cd backend
   node scripts/init-db.js
   ```
3. Проверьте, что папка `backend/data/` существует

### Фото не загружаются

**Проблема:** Ошибка при загрузке фото

**Решения:**
1. Проверьте размер файла (максимум 10MB)
2. Убедитесь, что формат файла - изображение (jpg, png, gif)
3. Проверьте права на запись в папку `backend/uploads/`
4. Проверьте логи: `backend/logs/error.log`

---

## Получение помощи

Если проблема не решена:

1. Проверьте логи:
   - Backend: `backend/logs/error.log` и `backend/logs/combined.log`
   - Frontend: консоль браузера (F12)

2. Убедитесь, что все переменные окружения установлены правильно

3. Проверьте документацию VK API: https://dev.vk.com/api

4. Убедитесь, что используете последние версии зависимостей:
   ```bash
   cd backend && npm update
   cd ../frontend && npm update
   ```

