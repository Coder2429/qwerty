# 🔧 Исправление проблемы с GitHub Pages

## Проблема
GitHub Pages показывает README.md вместо вашего приложения.

## Решение

### Шаг 1: Настройте GitHub Pages на использование GitHub Actions

1. Перейдите на GitHub: https://github.com/Coder2429/qwerty
2. Откройте **Settings** (Настройки)
3. В левом меню найдите **Pages**
4. В разделе **Source** (Источник) выберите:
   - **Source**: `GitHub Actions` (не "Deploy from a branch")
5. Сохраните изменения

### Шаг 2: Добавьте секреты (если ещё не добавили)

1. В репозитории перейдите: **Settings** → **Secrets and variables** → **Actions**
2. Нажмите **New repository secret**
3. Добавьте секрет:
   - **Name**: `BASE_PATH`
   - **Value**: `/qwerty/` (важно: с слешами в начале и конце!)
4. Нажмите **Add secret**
5. Добавьте второй секрет (если у вас уже есть VPS):
   - **Name**: `VITE_API_URL`
   - **Value**: `https://ваш-vps-домен.com` или `http://ваш-ip:5000`

### Шаг 3: Запустите workflow вручную

1. Перейдите в **Actions** (вкладка в репозитории)
2. Выберите workflow **"Deploy Frontend to GitHub Pages"**
3. Нажмите **Run workflow** (справа)
4. Выберите ветку **main**
5. Нажмите **Run workflow**

### Шаг 4: Дождитесь завершения

1. Дождитесь, пока workflow выполнится (обычно 1-2 минуты)
2. Если есть ошибки, нажмите на workflow и посмотрите логи
3. После успешного выполнения ваш сайт будет доступен по адресу:
   - `https://coder2429.github.io/qwerty/`

## ⚠️ Важно

- **BASE_PATH** должен быть `/qwerty/` (с слешами!) для репозитория "qwerty"
- Если репозиторий называется по-другому, измените BASE_PATH соответственно
- После первого успешного деплоя, при следующих push в main деплой будет запускаться автоматически

## 🔍 Проверка

После успешного деплоя:
1. Откройте `https://coder2429.github.io/qwerty/`
2. Вы должны увидеть ваше приложение, а не README.md
3. Если видите README, проверьте логи в GitHub Actions

## 🆘 Если не работает

1. Проверьте логи в **Actions** → выберите последний workflow → посмотрите ошибки
2. Убедитесь, что BASE_PATH указан правильно: `/qwerty/` (не `/qwerty` и не `qwerty/`)
3. Убедитесь, что в **Settings** → **Pages** выбран источник **GitHub Actions**

