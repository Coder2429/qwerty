# 🚀 Быстрый запуск проекта

## Предварительные требования

- Node.js 20+ 
- npm или yarn
- VK приложение (Mini App) с ID
- VK токен с правами: `photos`, `wall`, `groups`, `offline`

## Шаг 1: Клонирование и установка

```bash
# Установка зависимостей backend
cd backend
npm install

# Установка зависимостей frontend
cd ../frontend
npm install
```

## Шаг 2: Настройка Backend

```bash
cd backend

# Создайте файл .env
cp .env.example .env
```

Отредактируйте `.env`:
```env
PORT=5000
NODE_ENV=development
VK_ACCESS_TOKEN=ваш_токен_vk_здесь
FRONTEND_URL=http://localhost:3000
PAYMENT_TYPE=vk_pay
ORD_TYPE=vk
```

**Получение VK токена:**
1. Откройте (замените `ВАШ_APP_ID` на ID вашего приложения): 
   ```
   https://oauth.vk.com/authorize?client_id=ВАШ_APP_ID&display=page&redirect_uri=https://oauth.vk.com/blank.html&scope=4,8,268435456&response_type=token&v=5.131
   ```
   
   **⚠️ ВАЖНО:** Используйте числовые коды scope (`4,8,268435456`), а не текстовые названия!
2. Разрешите доступ
3. Скопируйте `access_token` из URL

## Шаг 3: Инициализация базы данных

```bash
cd backend
node scripts/init-db.js
```

База данных будет создана в `backend/data/orders.db`

## Шаг 4: Запуск Backend

```bash
cd backend
npm start
# или для разработки с автоперезагрузкой:
npm run dev
```

Сервер запустится на `http://localhost:5000`

## Шаг 5: Настройка Frontend

```bash
cd frontend

# Создайте файл .env
echo "VITE_API_URL=http://localhost:5000" > .env
```

## Шаг 6: Запуск Frontend

```bash
cd frontend
npm run dev
```

Приложение откроется на `http://localhost:3000`

## Шаг 7: Настройка VK приложения

1. Откройте https://dev.vk.com
2. Ваше приложение → Настройки
3. В поле "Базовый домен" укажите URL вашего фронтенда
   - Для разработки: используйте ngrok: `ngrok http 3000`
   - Для продакшена: укажите домен Vercel/другого хостинга
4. Включите VK Pay в разделе "Платежи"

## Шаг 8: Настройка кнопки в группе

1. Откройте вашу группу ВКонтакте
2. Управление → Настройки → Кнопка сообщества
3. Выберите "Открыть мини-приложение"
4. Найдите ваше приложение по ID
5. Сохраните

## ✅ Готово!

Теперь вы можете:
- Открыть приложение через кнопку в группе
- Создать пост с текстом и фото
- Оплатить через VK Pay
- Пост автоматически опубликуется с ERID

## 🐛 Решение проблем

### Backend не запускается
- Проверьте, что порт 5000 свободен
- Убедитесь, что установлены все зависимости: `npm install`
- Проверьте файл `.env` и токен VK

### Frontend не подключается к Backend
- Проверьте `VITE_API_URL` в `.env` фронтенда
- Убедитесь, что backend запущен
- Проверьте CORS настройки в backend

### Ошибка при публикации поста
- Проверьте права токена VK (должны быть: photos, wall, groups)
- Убедитесь, что токен имеет права администратора группы
- Проверьте логи: `backend/logs/error.log`

### VK Pay не работает
- Убедитесь, что VK Pay включён в настройках приложения
- Проверьте, что приложение опубликовано (не в режиме разработки)
- Проверьте формат заказа в логах

## 📝 Проверка работы

### Тест Backend API:

```bash
# Health check
curl http://localhost:5000/health

# Должен вернуть:
# {"status":"ok","timestamp":"...","service":"paypost-backend","version":"1.0.0"}
```

### Тест Frontend:

Откройте `http://localhost:3000` - должна загрузиться форма создания поста.

### Тест в VK:

1. Откройте приложение через кнопку в группе
2. Заполните форму
3. Оплатите (в тестовом режиме VK Pay)
4. Проверьте, что пост опубликовался в группе с ERID

## 📊 Логи

- Backend логи: `backend/logs/combined.log`
- Ошибки: `backend/logs/error.log`
- В консоли также выводятся логи в режиме разработки

## 🔄 Следующие шаги

После успешного запуска:
1. Настройте деплой на VPS (см. DEPLOY.md)
2. Настройте деплой фронтенда на Vercel
3. Настройте реальный ОРД API (см. ORD_INTEGRATION.md)
4. Настройте мониторинг и резервное копирование

