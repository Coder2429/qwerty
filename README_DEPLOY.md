# 📦 Инструкция по деплою проекта

## Что было сделано

✅ Настроен фронтенд для деплоя на GitHub Pages  
✅ Создан GitHub Actions workflow для автоматического деплоя  
✅ Подготовлены инструкции по деплою бекенда на VPS  
✅ Настроен Vite для работы с GitHub Pages  

## 🎯 Что нужно сделать вам

### 1. Фронтенд на GitHub Pages

**Шаг 1:** Создайте репозиторий на GitHub (если ещё нет)

**Шаг 2:** Загрузите код в репозиторий:
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git
git push -u origin main
```

**Шаг 3:** В настройках репозитория GitHub:
- Перейдите в **Settings** → **Pages**
- В разделе **Source** выберите **GitHub Actions**

**Шаг 4:** Настройте секреты:
- Перейдите в **Settings** → **Secrets and variables** → **Actions**
- Добавьте секрет `VITE_API_URL` со значением URL вашего VPS (например: `https://api.yourdomain.com` или `http://your-ip:5000`)
- Если репозиторий не в корне GitHub Pages, добавьте `BASE_PATH` (например: `/Vkbot/`)

**Шаг 5:** После push в ветку `main` деплой запустится автоматически!

Ваш фронтенд будет доступен по адресу:  
`https://ВАШ_USERNAME.github.io/ВАШ_РЕПОЗИТОРИЙ/`

---

### 2. Бекенд на VPS

**Быстрый способ:** Следуйте инструкциям в файле `QUICK_DEPLOY.md`

**Подробный способ:** Следуйте инструкциям в файле `DEPLOY_GUIDE.md` (раздел "Деплой бекенда на VPS")

**Кратко:**
1. Подключитесь к VPS по SSH
2. Установите Node.js и PM2
3. Загрузите папку `backend` на сервер
4. Установите зависимости: `npm install --production`
5. Создайте файл `.env` с настройками (см. `DEPLOY_GUIDE.md`)
6. Запустите: `pm2 start src/index.js --name vkbot-backend`
7. Настройте Nginx (опционально, но рекомендуется)

---

## ⚙️ Важные настройки

### В бекенде (.env на VPS):
```env
FRONTEND_URL=https://ваш-username.github.io/ваш-репозиторий
VK_ACCESS_TOKEN=ваш_токен
```

### В GitHub Secrets:
```
VITE_API_URL=https://ваш-vps-домен.com
```

---

## 📚 Документация

- **QUICK_DEPLOY.md** - быстрая инструкция (5-10 минут)
- **DEPLOY_GUIDE.md** - подробное руководство со всеми шагами
- **README.md** - общая информация о проекте

---

## ❓ Вопросы?

Если что-то не работает:
1. Проверьте логи бекенда: `pm2 logs vkbot-backend`
2. Проверьте GitHub Actions в репозитории
3. Убедитесь, что переменные окружения настроены правильно
4. Проверьте, что CORS настроен правильно (FRONTEND_URL в бекенде должен совпадать с URL GitHub Pages)

---

**Удачи с деплоем! 🚀**

