# 📦 Установка зависимостей

## Проблема: "Cannot find package 'express'"

Эта ошибка означает, что зависимости не установлены. Нужно выполнить `npm install`.

## Решение для Windows

### Вариант 1: Использовать CMD (рекомендуется)

1. Откройте **Командную строку (CMD)**, а не PowerShell
   - Нажмите `Win + R`
   - Введите `cmd` и нажмите Enter

2. Перейдите в папку backend:
   ```cmd
   cd "D:\Рабочии стол\Vkbot\backend"
   ```

3. Установите зависимости:
   ```cmd
   npm install
   ```

4. Дождитесь завершения установки (может занять 1-2 минуты)

### Вариант 2: Использовать PowerShell (если CMD недоступен)

Если у вас проблемы с PowerShell, выполните:

1. Откройте PowerShell от имени администратора
2. Разрешите выполнение скриптов (один раз):
   ```powershell
   Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
   ```

3. Перейдите в папку backend:
   ```powershell
   cd "D:\Рабочии стол\Vkbot\backend"
   ```

4. Установите зависимости:
   ```powershell
   npm install
   ```

### Вариант 3: Использовать Git Bash (если установлен Git)

1. Откройте Git Bash
2. Выполните:
   ```bash
   cd "/d/Рабочии стол/Vkbot/backend"
   npm install
   ```

## Что будет установлено

После выполнения `npm install` будут установлены следующие пакеты:

- **express** - веб-сервер
- **dotenv** - работа с переменными окружения
- **cors** - обработка CORS запросов
- **multer** - загрузка файлов
- **axios** - HTTP запросы
- **form-data** - работа с формами
- **better-sqlite3** - база данных SQLite
- **winston** - логирование

## Проверка установки

После установки проверьте, что всё установлено:

```cmd
cd "D:\Рабочии стол\Vkbot\backend"
npm list --depth=0
```

Должны быть видны все установленные пакеты.

## Установка зависимостей для Frontend

После установки backend зависимостей, установите зависимости для frontend:

1. Откройте CMD
2. Выполните:
   ```cmd
   cd "D:\Рабочии стол\Vkbot\frontend"
   npm install
   ```

## После установки

После успешной установки всех зависимостей:

1. Убедитесь, что создан файл `backend/.env` с вашим токеном
2. Запустите backend:
   ```cmd
   cd "D:\Рабочии стол\Vkbot\backend"
   npm start
   ```

3. В другом окне терминала запустите frontend:
   ```cmd
   cd "D:\Рабочии стол\Vkbot\frontend"
   npm run dev
   ```

## Если npm не найден

Если команда `npm` не найдена:

1. Убедитесь, что Node.js установлен:
   ```cmd
   node --version
   ```

2. Если Node.js не установлен, скачайте с https://nodejs.org/
   - Рекомендуется версия 20.x или выше

3. После установки Node.js перезапустите терминал

## Решение проблем

### Ошибка "npm не является внутренней или внешней командой"

**Решение:** Node.js не установлен или не добавлен в PATH. Переустановите Node.js.

### Ошибка "EACCES: permission denied"

**Решение:** Запустите терминал от имени администратора.

### Ошибка "network timeout"

**Решение:** Проверьте интернет-соединение или используйте другой npm registry:
```cmd
npm config set registry https://registry.npmjs.org/
```

### Ошибка при установке better-sqlite3 (gyp ERR! find VS)

**Проблема:** `better-sqlite3` требует компиляции нативных модулей, для чего нужны Visual Studio Build Tools.

**Решение 1: Установить Visual Studio Build Tools (рекомендуется)**

1. Скачайте **Visual Studio Build Tools**:
   - Перейдите на https://visualstudio.microsoft.com/downloads/
   - Скачайте "Build Tools for Visual Studio 2022" (бесплатно)

2. При установке выберите:
   - **"Desktop development with C++"** (рабочая нагрузка)
   - Это установит необходимые инструменты компиляции

3. После установки перезапустите терминал и попробуйте снова:
   ```cmd
   cd "D:\Рабочии стол\Vkbot\backend"
   npm install
   ```

**Решение 2: Использовать предкомпилированные бинарники (быстрое решение)**

Если не хотите устанавливать Visual Studio, попробуйте:

```cmd
cd "D:\Рабочии стол\Vkbot\backend"
npm install --build-from-source=false better-sqlite3
```

Если это не сработает, попробуйте установить только необходимые пакеты без better-sqlite3, а затем установить его отдельно с флагами:

```cmd
npm install express dotenv cors multer axios form-data winston
npm install better-sqlite3 --build-from-source=false --fallback-to-build=false
```

**Решение 3: Использовать альтернативную БД (временное решение)**

Если ничего не помогает, можно временно заменить better-sqlite3 на более простую реализацию, но это потребует изменения кода.

