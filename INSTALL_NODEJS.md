# 📥 Установка Node.js на Windows

## Проблема: "node не является внутренней или внешней командой"

Эта ошибка означает, что Node.js не установлен или не добавлен в PATH.

## ✅ Решение: Установить Node.js 20.x LTS

### Шаг 1: Скачать Node.js

1. Откройте браузер и перейдите на **https://nodejs.org/**
2. На главной странице вы увидите две кнопки:
   - **LTS** (Long Term Support) - рекомендуется
   - **Current** - последняя версия
3. Нажмите на кнопку **LTS** (обычно это Node.js 20.x)
4. Скачайте установщик для Windows:
   - Выберите **Windows Installer (.msi)** для 64-bit
   - Файл будет называться примерно: `node-v20.18.0-x64.msi`

### Шаг 2: Установить Node.js

1. Запустите скачанный файл `.msi`
2. Нажмите **"Next"** на экране приветствия
3. Примите лицензионное соглашение и нажмите **"Next"**
4. Выберите путь установки (по умолчанию: `C:\Program Files\nodejs\`) и нажмите **"Next"**
5. **ВАЖНО:** Оставьте галочку **"Automatically install the necessary tools"** включенной
6. Нажмите **"Next"** и затем **"Install"**
7. Дождитесь завершения установки
8. Нажмите **"Finish"**

### Шаг 3: Перезапустить терминал

**ОБЯЗАТЕЛЬНО:** После установки Node.js:

1. **Закройте все окна терминала/CMD/PowerShell**
2. Откройте **новое** окно CMD или PowerShell
3. Это необходимо, чтобы система обновила переменные окружения PATH

### Шаг 4: Проверить установку

В новом терминале выполните:

```cmd
node --version
```

Должно показать: `v20.x.x` (например, `v20.18.0`)

```cmd
npm --version
```

Должно показать версию npm (например, `10.x.x`)

Если команды работают - Node.js установлен правильно! ✅

### Шаг 5: Установить зависимости проекта

Теперь можно установить зависимости:

```cmd
cd "D:\Рабочии стол\Vkbot\backend"
npm install
```

---

## 🔍 Если команды всё ещё не работают

### Проблема 1: Node.js не в PATH

1. Нажмите `Win + R`
2. Введите `sysdm.cpl` и нажмите Enter
3. Перейдите на вкладку **"Дополнительно"**
4. Нажмите **"Переменные среды"**
5. В разделе **"Системные переменные"** найдите переменную **"Path"**
6. Нажмите **"Изменить"**
7. Проверьте, есть ли там путь: `C:\Program Files\nodejs\`
8. Если нет - нажмите **"Создать"** и добавьте: `C:\Program Files\nodejs\`
9. Нажмите **"OK"** везде
10. **Перезагрузите компьютер** или перезапустите терминал

### Проблема 2: Node.js установлен в другом месте

1. Найдите, где установлен Node.js:
   - Обычно: `C:\Program Files\nodejs\`
   - Или: `C:\Program Files (x86)\nodejs\`
   - Или: `C:\Users\ВашеИмя\AppData\Local\nodejs\`

2. Добавьте этот путь в переменную PATH (см. выше)

### Проблема 3: Переустановка

Если ничего не помогает:

1. Удалите Node.js через "Программы и компоненты"
2. Перезагрузите компьютер
3. Скачайте и установите Node.js заново (см. Шаг 1-2)
4. **Обязательно перезапустите терминал** после установки

---

## ✅ Проверка после установки

После установки Node.js выполните:

```cmd
node --version
npm --version
```

Обе команды должны работать и показывать версии.

Затем установите зависимости:

```cmd
cd "D:\Рабочии стол\Vkbot\backend"
npm install
```

---

## 📝 Примечания

- **Node.js 20.x LTS** - рекомендуется для большинства проектов
- После установки **обязательно перезапустите терминал**
- Если команды не работают - проверьте PATH (см. выше)
- Node.js включает npm автоматически - устанавливать отдельно не нужно

