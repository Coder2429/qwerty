# Интеграция с ОРД (Оператор рекламных данных)

## 📋 Обзор

Система автоматически интегрирована с ОРД для соблюдения требований законодательства РФ о рекламе. Все рекламные посты регистрируются в ОРД и получают ERID (Единый рекламный идентификатор), который автоматически добавляется в публикуемый пост.

## ✅ Что реализовано

### 1. Автоматическая регистрация в ОРД

- Перед публикацией поста система автоматически регистрирует рекламу в ОРД
- Получает ERID от ОРД
- Добавляет ERID в текст поста в формате: `Реклама\nERID: XXXXXXXX`

### 2. Поддержка собственного ERID

- Если у рекламодателя уже есть ERID, он может указать его в форме
- Система использует указанный ERID без дополнительной регистрации
- Валидация формата ERID перед использованием

### 3. Формат поста с ERID

После публикации пост имеет следующий формат:

```
Реклама
ERID: 12345678-ABCD-1234-EFGH-123456789012

[Ваш текст поста здесь]
```

## 🔧 Настройка

### Переменные окружения

Добавьте в `backend/.env`:

```env
# Тип ОРД (по умолчанию 'vk' - VK ОРД)
ORD_TYPE=vk

# Токен для работы с ОРД (если отличается от основного VK токена)
# По умолчанию используется VK_ACCESS_TOKEN
VK_ORD_TOKEN=your_ord_token_here
```

### VK ОРД API

Система использует ОРД API для регистрации рекламы:

- **Метод**: `ads.registerAd` (пример - адаптируйте под реальный API)
- **Документация VK Ads**: https://dev.vk.com/api/ads
- **Требования**: Токен с правами для работы с рекламой

**⚠️ Важно**: Реализация в `backend/src/ord.js` является примером. Вам необходимо:

1. **Проверить реальный API вашего ОРД** - VK может не предоставлять публичный метод для регистрации в ОРД
2. **Использовать официальный API ОРД** - например, через erid.ru или другой ОРД
3. **Адаптировать код** под реальный API выбранного ОРД

Альтернативные варианты:
- Интеграция с официальным ОРД через их API
- Использование VK Ads API (если доступен)
- Интеграция с другими ОРД (если требуется)

## 📝 Как это работает

### Процесс публикации поста:

1. **Пользователь создаёт пост и оплачивает**
   - Заполняет форму (текст, фото, цена)
   - Опционально указывает свой ERID
   - Оплачивает через VK Pay

2. **Система обрабатывает оплату**
   - Подтверждает платеж
   - Если ERID не указан → регистрирует рекламу в ОРД
   - Если ERID указан → использует его без регистрации

3. **Получение ERID**
   - Вызывается `registerAdInOrd()` из `backend/src/ord.js`
   - Для VK ОРД используется метод `ads.registerAd`
   - Получается ERID от ОРД

4. **Форматирование поста**
   - Текст поста дополняется блоком "Реклама\nERID: XXXXXXXX"
   - ERID добавляется в начало поста

5. **Публикация**
   - Пост публикуется в группе с ERID
   - Сохраняется информация о ERID для отчётности

## 🔄 Fallback механизм

Если ОРД API недоступен или возвращает ошибку:

- Система генерирует временный ERID (формат: `TEMP-{timestamp}-{hash}`)
- Пост всё равно публикуется с временным ERID
- Ошибка логируется для последующей обработки

**⚠️ Важно**: В продакшене временный ERID должен быть заменён на реальную регистрацию. Убедитесь, что:
- VK ОРД API доступен
- Токен имеет необходимые права
- Обрабатываются все ошибки ОРД

## 📊 Отчётность

Система сохраняет ERID для каждого заказа:

```javascript
{
  order_id: "order_123",
  erid: "12345678-ABCD-1234-EFGH-123456789012",
  published_at: "2024-01-15T10:30:00.000Z",
  // ...
}
```

Это позволяет:
- Отслеживать все опубликованные рекламные посты
- Формировать отчёты для ОРД
- Проверять соответствие требованиям законодательства

## 🛠️ Интеграция с другими ОРД

Система спроектирована для поддержки разных ОРД. Для добавления нового ОРД:

1. Обновите `backend/src/ord.js`
2. Добавьте функцию регистрации для нового ОРД
3. Обновите `registerAdInOrd()` для поддержки нового типа
4. Установите `ORD_TYPE` в переменных окружения

Пример:

```javascript
// В ord.js
if (ord_type === 'vk') {
  return await registerInVkOrd(data)
} else if (ord_type === 'other_ord') {
  return await registerInOtherOrd(data)
}
```

## ✅ Соответствие законодательству

Система обеспечивает:

- ✅ Автоматическую регистрацию рекламы в ОРД
- ✅ Получение ERID перед публикацией
- ✅ Добавление ERID в пост в требуемом формате
- ✅ Сохранение данных для отчётности
- ✅ Поддержку собственного ERID рекламодателя

## 🔍 Проверка работы

### Тест регистрации в ОРД:

```bash
# Проверьте логи backend при публикации поста
pm2 logs paypost-backend

# Должны увидеть:
# "Регистрация в ОРД: успешно, ERID: XXXXXXXX"
```

### Проверка формата поста:

После публикации проверьте пост в группе - он должен начинаться с:
```
Реклама
ERID: XXXXXXXX
```

## 📚 Полезные ссылки

- [Закон "О рекламе"](https://www.consultant.ru/document/cons_doc_LAW_58968/)
- [VK ОРД API](https://dev.vk.com/api/ads)
- [Требования к маркировке рекламы](https://erid.ru/)

## ⚠️ Важные замечания

1. **Токен должен иметь права для работы с рекламой**
   - При получении токена VK укажите необходимые scope
   - Для VK ОРД может потребоваться отдельный токен

2. **В продакшене отключите fallback на временный ERID**
   - Убедитесь, что ОРД API всегда доступен
   - Обрабатывайте ошибки ОРД отдельно

3. **Храните ERID для отчётности**
   - Сохраняйте ERID в базе данных
   - Ведите учёт всех опубликованных рекламных постов

4. **Проверяйте формат ERID**
   - ERID должен соответствовать требованиям ОРД
   - Валидируйте ERID перед использованием

