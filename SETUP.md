# Пошаговая инструкция по настройке

## 📋 Шаг 1: Регистрация приложения в VK

1. Перейдите на https://dev.vk.com
2. Войдите в свой аккаунт ВКонтакте
3. Нажмите **"Создать приложение"**
4. Выберите тип: **"Мини-приложение"**
5. Заполните:
   - **Название**: "Платный пост в сообществе" (или любое другое)
   - **Базовый домен**: пока оставьте пустым (заполните после деплоя)
6. Нажмите **"Создать"**
7. **Сохраните ID приложения** (он понадобится позже)

### Включение VK Pay

1. В настройках приложения перейдите в раздел **"Платежи"**
2. Включите **"VK Pay"**
3. Заполните информацию о продавце (если требуется)
4. Сохраните изменения

---

## 🔑 Шаг 2: Получение токена VK API

Вам нужен токен с правами: `photos`, `wall`, `groups`, `offline`

### Способ 1: Через OAuth (рекомендуется)

1. Откройте в браузере (замените `YOUR_APP_ID` на ID вашего приложения):
   ```
   https://oauth.vk.com/authorize?client_id=YOUR_APP_ID&display=page&redirect_uri=https://oauth.vk.com/blank.html&scope=4,8,268435456&response_type=token&v=5.131
   ```
   
   **⚠️ ВАЖНО:** Используйте числовые коды scope:
   - `4` = photos (фотографии)
   - `8` = wall (стена)
   - `268435456` = offline (офлайн доступ)
   
   Не используйте текстовые названия (`photos,wall,offline`) - это вызовет ошибку "invalid scope"!

2. Разрешите доступ приложению

3. После редиректа в URL будет параметр `access_token=...`
   - Скопируйте значение токена
   - ⚠️ **Сохраните его в безопасном месте!**

### Способ 2: Через VK API (для тестирования)

Можно использовать токен из консоли разработчика:
1. Откройте https://dev.vk.com
2. Ваше приложение → **"Инструменты"** → **"API-консоль"**
3. Выберите метод `wall.post` и получите токен (но он будет временным)

---

## 💻 Шаг 3: Локальная разработка

### Настройка Frontend

```bash
# Перейдите в папку frontend
cd frontend

# Установите зависимости
npm install

# Создайте файл .env
echo "VITE_API_URL=http://localhost:5000" > .env

# Запустите dev-сервер
npm run dev
```

Откройте http://localhost:3000

### Настройка Backend

```bash
# В новом терминале перейдите в папку backend
cd backend

# Установите зависимости
npm install

# Создайте файл .env
nano .env
```

Вставьте в `.env`:
```env
PORT=5000
NODE_ENV=development
VK_ACCESS_TOKEN=ваш_токен_здесь
FRONTEND_URL=http://localhost:3000
PAYMENT_TYPE=vk_pay
```

Сохраните (Ctrl+O, Enter, Ctrl+X)

```bash
# Запустите сервер
npm start
# или для автоперезагрузки:
npm run dev
```

---

## 🧪 Шаг 4: Тестирование локально

### Тест без VK (для разработки UI)

1. Откройте http://localhost:3000
2. Форма должна работать (но без реальной публикации в VK)

### Тест с VK Mini App

1. В настройках приложения VK укажите временный URL:
   - Можно использовать ngrok: `ngrok http 3000`
   - Или Vercel Preview (если уже задеплоили)

2. Откройте приложение через VK:
   - Создайте тестовую группу
   - Настройте кнопку (см. Шаг 5)
   - Откройте приложение через кнопку

---

## 🔘 Шаг 5: Настройка кнопки в группе

1. Откройте вашу группу ВКонтакте
2. Перейдите: **"Управление"** → **"Настройки"** → **"Кнопка сообщества"**
3. Выберите действие: **"Открыть мини-приложение"**
4. В поиске найдите ваше приложение (по ID или названию)
5. Нажмите **"Сохранить"**

Теперь кнопка будет открывать ваше Mini App!

---

## 🚀 Шаг 6: Деплой

См. файл [DEPLOY.md](./DEPLOY.md) для подробных инструкций.

### Краткая версия:

**Frontend:**
```bash
cd frontend
npm i -g vercel
vercel
```

**Backend:**
- Загрузите на VPS
- Установите Node.js и PM2
- Настройте `.env`
- Запустите через PM2

---

## ✅ Шаг 7: Финальная настройка

1. **Обновите базовый домен в VK:**
   - https://dev.vk.com → Ваше приложение → Настройки
   - Укажите домен Vercel (например: `paypost-miniapp.vercel.app`)

2. **Обновите переменные окружения:**
   - Frontend: `VITE_API_URL=https://your-backend-domain.com`
   - Backend: `FRONTEND_URL=https://your-frontend-domain.vercel.app`

3. **Протестируйте полный цикл:**
   - Откройте приложение через кнопку в группе
   - Заполните форму
   - Оплатите через VK Pay
   - Проверьте, что пост опубликовался

---

## 🐛 Решение проблем

### Проблема: "VK_ACCESS_TOKEN не установлен"

**Решение:** Проверьте файл `.env` в папке `backend` и убедитесь, что токен указан правильно.

### Проблема: "Ошибка при публикации поста"

**Возможные причины:**
1. Токен не имеет нужных прав → получите новый токен с правами `photos,wall,groups,offline`
2. Группа не существует или недоступна → проверьте `group_id`
3. У токена нет прав администратора группы → добавьте токен как администратор группы

### Проблема: "CORS error"

**Решение:** Убедитесь, что в `backend/.env` указан правильный `FRONTEND_URL`.

### Проблема: VK Pay не работает

**Проверьте:**
1. VK Pay включён в настройках приложения
2. Приложение опубликовано (не в режиме разработки)
3. Используете правильный формат заказа

### Проблема: Фото не загружаются

**Проверьте:**
1. Размер файла не превышает 10MB
2. Формат файла - изображение (jpg, png и т.д.)
3. Токен имеет право `photos`

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи backend: `pm2 logs paypost-backend`
2. Проверьте консоль браузера (F12)
3. Убедитесь, что все переменные окружения установлены
4. Проверьте документацию VK API: https://dev.vk.com/api

---

## 🎉 Готово!

После выполнения всех шагов ваше приложение должно работать. Удачи! 🚀

